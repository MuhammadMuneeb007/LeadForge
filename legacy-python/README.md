# Legacy Python prototype

This is the original local FastAPI and Playwright prototype. It is retained for
reference and is no longer the hosted production architecture.

The deployable LeadForge application now lives at the repository root and uses
Next.js Route Handlers plus external business-data APIs. The hosted application
does not require Python, FastAPI, Playwright, Chromium, SMTP, JSON job storage,
or Docker.

